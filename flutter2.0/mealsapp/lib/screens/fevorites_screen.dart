import 'package:flutter/material.dart';

class FevoritesScreen extends StatelessWidget {
  // const FevoritesScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Fevorites'),
    );
  }
}
